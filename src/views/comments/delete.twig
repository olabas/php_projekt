{% extends 'base.twig' %}

{% block title %} {{ 'Find your teacher'|trans }} / {{ 'Login panel'|trans }} {% endblock %}

{% block content %}
    <div class="panel panel-default contain">
        <div class="panel-heading">
            <h3 class="panel-title title">{{ 'Delete comment'|trans }}</h3>
        </div>
        <div class="panel-body title">
         <h4>Are you sure you want to delete this post?</h4>
         <h5> The post will be permanently deleted.</h5>
           <form action="{{ path('comments_delete', {'id': id }) }}" method="post">
        <a href="{{ url('index') }}" class="btn btn-primary">{{ 'NO'|trans }}</a>
        <input class="btn btn-inverse" type="submit" name="submit" value="{{ 'YES'|trans }}" />
    </form>
        </div>
    </div>
{% endblock %}

